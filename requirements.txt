# GI Data Lakehouse — Production dependencies
# Python 3.11+

# Core pipeline
pyodbc==5.1.*
boto3==1.34.*
psycopg2-binary==2.9.*
duckdb==1.1.*

# Quality
great-expectations==0.18.*

# Orchestration (Airflow managed on OVHcloud, local dev only)
# apache-airflow==2.8.*

# dbt (optional — alternative to Python Gold scripts)
# dbt-postgres==1.7.*

# Dev & CI
ruff==0.3.*
mypy==1.9.*
pytest==8.1.*
pytest-cov==5.0.*

# Load testing
locust==2.24.*
